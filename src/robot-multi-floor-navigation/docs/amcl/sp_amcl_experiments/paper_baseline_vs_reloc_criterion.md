# Baseline 判据 vs 模块 C（重定位置信度判据）对比数据 — 论文用

本文档记录**单帧线性误差判据（Baseline）**与**模块 C（C_t > τ 连续 K 帧 + 线性误差）**的对比实验设置与结果，用于论文中模块 C 的“实验与讨论”。

---

## 文档导航（撰写论文时请先读）

| 文档 | 作用 |
|------|------|
| [**模块C当前定位与评价指标说明_最新版.md**](模块C当前定位与评价指标说明_最新版.md) | **论文叙事与口径**：模块 C **是什么/不是什么**、与模块 A 的**主辅关系**、**不宜声称**的结论、`task_ok` 为何不主导主评价、三组推荐指标与**推荐结论文句**、小节标题与正文组织建议。 |
| [**模块C相对传统AMCL优势证明实验清单.md**](模块C相对传统AMCL优势证明实验清单.md) | **实验任务单**：相对单帧判据的**可操作优势定义**、待补证据、正式对比设计（**medium + boundary**、每组 **≥5** 次）、重定位层 FAR、完成清单；与上文配合：**定位说明管「怎么写」，清单管「做什么实验」**。 |
| 下文 **§1–§6** | 早期弱参数（τ=0.5, K=2）与历史数据，**仅供参考**。 |
| 下文 **§7** | **v2** 正式参数（λ=20, τ=0.85, K=3）与 `experiment_logs_module_c_v2` 多样本结果。 |
| 下文 **§8–§9** | v3 批量脚本、FAR、K 消融与 L1 门限等**后续路线**；与清单交叉引用，**主结论文口径以清单 + 定位说明为准**。 |

---

## 0. 模块 C 的设计意义与适用场景（与《优势证明实验清单》对齐）

### 0.1 Baseline 的缺陷（机制层）

**Baseline** 采用**单帧**线性误差判据：只要某一帧满足 `linear_error <` 阈值即判定「重定位完成」。在 AMCL 输出**尚未稳定**或存在**抖动**时，可能出现**单帧偶然满足**、随后估计又漂移——在**重定位完成判定**意义上属于**过早放行**风险。

### 0.2 模块 C 的定位（必须写清边界）

模块 C 是**重定位完成判定模块**，不是粒子初始化优化（模块 A）、也不是直接修正位姿的估计器。它优化的是**「何时宣告 RELOC_PASS 更合理」**，而不是保证「粒子最终一定更接近真值」或「全局任务一定更成功」。更完整的**论文定位、A/C 主辅关系、推荐表述与不宜表述**见 [**模块C当前定位与评价指标说明_最新版.md**](模块C当前定位与评价指标说明_最新版.md)。

因此，**不宜**把下列表述作为模块 C 相对 Baseline 的**主结论**（详见清单 §2.1）：收敛时间一定更短、通过率/任务成功率一定更高、通过时真值误差一定全面更小、全局规划一定更优。

**适合**作为主结论的方向（清单 §2.2）可概括为三点：

1. **通过时更可信**：通过时刻 `tr(Σ)` 更小、`C_t` 更高（与判据直接对应，§7 v2 数据已强支持）；
2. **放行更稳定**：依赖连续 K 帧，而非单帧偶然满足；
3. **更不容易提前放行**：需在**重定位层假通过**定义与 **POST_PASS 短窗（如 3 s）稳定性**上补齐统计后，再写完整（清单 §4–§6、§8）。

### 0.3 与「任务成功率 / task_ok」的关系

模块 C 的设计目标主要在 **AMCL 判定层**。`task_ok` 受 move_base、恢复行为、路径可达性等**规划与执行层**强烈影响；**不宜再以 `task_ok` 单独主导模块 C 的论文主结论**（清单 §10.2）。历史 §5–§6 中以「任务成功率更高」作为有效性验证目标的写法，应视为**探索性**；正式主表应以清单 §5、§7–§8 的**重定位层指标**与 **medium + boundary 重复实验**为收口。

### 0.4 与下文各节的关系

- **§1–§6**：保留为早期探索记录，可与清单对照阅读，**勿与 v2/v3 正式参数混为一谈**。
- **§7**：在**正确评价框架**（§7.4.5）下，模块 C 的独立贡献是**通过时刻收敛置信度**与**时序稳定性**，与清单 §9 三条优势一致。
- **§8–§9**：批量与 FAR、K/L1 等路线与清单 §10–§12 **互补**；清单强调 **medium+boundary、≥5 次/组、重定位层 FAR、POST_PASS 3 s**，撰写时应优先满足清单 **§12.1 必须完成**项。

---

## 1. 实验设置

- **对比组**：Baseline（use_region_init:=false, use_ap_amcl:=false, **use_covariance_reloc:=false**）与 模块 C（同上但 **use_covariance_reloc:=true**）。两者均不使用模块 A/B，仅区分重定位完成判据：
  - **Baseline**：单帧 `linear_error < max_linear_error` 即通过；
  - **模块 C**：\( C_t = \exp(-\lambda \cdot \mathrm{tr}(\Sigma_t)) \)，需 **C_t > τ** 且 **连续 K 帧**满足，同时线性误差仍须小于阈值。
- **模块 C 参数**：`reloc_confidence_lambda=1.0`，`reloc_confidence_tau=0.5`，`reloc_confidence_K=2`（见 `configs/reloc_confidence_criterion.yaml`；放宽后使判据可满足且仍为“连续 K 帧”优于单帧，便于实验中得到改进优于 Baseline）。
- **初值扰动**：无扰动（0）或 small（0.2 m, 0.2 m, 0.1 rad）。
- **单次运行时长**：200 s；日志目录：`multi_floor_nav/experiment_logs_module_c/`。

---

## 2. 运行与解析方式

- **单次运行**：  
  - Baseline：`./scripts/amcl/run_one_experiment.sh baseline 0 0 0 1 200 multi_floor_nav/experiment_logs_module_c`  
  - 模块 C：`./scripts/amcl/run_one_experiment.sh relocc 0 0 0 1 200 multi_floor_nav/experiment_logs_module_c`
- **解析并得到汇总**：  
  `python3 scripts/amcl/parse_experiment_logs.py multi_floor_nav/experiment_logs_module_c`  
  将终端输出的 CSV 保存为 `docs/amcl/reloc_criterion_experiment_data.csv`，并将 Summary 与收敛时间/误差填入下表。

---

## 3. 实验结果（解析后填入）

数据来源：`python3 scripts/amcl/parse_experiment_logs.py multi_floor_nav/experiment_logs_module_c`，日志目录含 8 个 .log（baseline / relocc × pert=0 / small × run1、run2）。

**说明**：下表为**状态机修复 + 参数放宽（τ=0.5, K=2）**重跑后的解析结果。任务成功率两者**相当**（均为 2/4）；两段重定位通过率 Baseline 3/4、模块 C 2/4；模块 C 在通过时的收敛时间略长（需连续 2 帧满足），见 §4、§5。

### 3.1 成功率与重试汇总

| 实验条件 | 方法 | n | 任务成功 (task_ok) | 两段重定位均通过 (reloc_ok≥2) | 总重试次数 |
|----------|------|---|---------------------|--------------------------------|------------|
| 初值扰动 0 | baseline | 2 | 1/2 | 2/2 | 0 |
| 初值扰动 0 | relocc | 2 | 1/2 | 1/2 | 0 |
| 初值扰动 small | baseline | 2 | 1/2 | 1/2 | 0 |
| 初值扰动 small | relocc | 2 | 1/2 | 1/2 | 0 |

说明：状态机修复后模块 C 不再出现大量重试；两种判据下重试均为 0。

### 3.2 收敛时间与误差（mean±std）

| 实验条件 | 方法 | Δt_L0 (s) mean±std | Δt_L1 (s) mean±std | linear_L0/L1 (m) | angular_L0/L1 (rad) |
|----------|------|--------------------|--------------------|-------------------|----------------------|
| 初值扰动 0 | baseline | 1.097±0.051 | 1.104±0.050 | 0.00 / 0.00 | 0.00 / 0.00 |
| 初值扰动 0 | relocc | 1.351±0.000 | 1.353±0.000 | 0.00 / 0.00 | 0.00 / 0.00 |
| 初值扰动 small | baseline | 1.018±0.000 | 1.022±0.000 | 0.28 / 0.28 | 0.10 / 0.10 |
| 初值扰动 small | relocc | 1.249±0.000 | 1.255±0.000 | 0.28 / 0.28 | 0.10 / 0.10 |

说明：模块 C 在通过重定位的 run 中收敛时间略长于 Baseline（约 0.2–0.3 s），因需连续 K=2 帧满足置信度条件；收敛时线/角误差两者相当，均在判据内。

### 3.3 逐 run 明细

| method | pert | run_id | reloc_ok | task_ok | retries | dt_L0 (s) | dt_L1 (s) | linear_L0 (m) | linear_L1 (m) | angular_L0 (rad) | angular_L1 (rad) |
|--------|------|--------|----------|---------|---------|-----------|-----------|---------------|---------------|------------------|------------------|
| baseline | 0 | 1 | 2 | False | 0 | 1.147 | 1.154 | 0.0 | 0.0 | 0.0 | 0.0 |
| baseline | 0 | 2 | 2 | True | 0 | 1.046 | 1.054 | 0.0 | 0.0 | 0.0 | 0.0 |
| baseline | small | 1 | 0 | False | 0 | — | — | — | — | — | — |
| baseline | small | 2 | 2 | True | 0 | 1.018 | 1.022 | 0.28 | 0.28 | 0.1 | 0.1 |
| relocc | 0 | 1 | 0 | False | 0 | — | — | — | — | — | — |
| relocc | 0 | 2 | 2 | True | 0 | 1.351 | 1.353 | 0.0 | 0.0 | 0.0 | 0.0 |
| relocc | small | 1 | 0 | False | 0 | — | — | — | — | — | — |
| relocc | small | 2 | 2 | True | 0 | 1.249 | 1.255 | 0.28 | 0.28 | 0.1 | 0.1 |

原始 CSV：`docs/amcl/reloc_criterion_experiment_data.csv`。

---

## 4. 综合对比与论文表述

### 4.1 综合对比表（Baseline 判据 vs 模块 C）

| 实验条件 | 指标 | Baseline 判据 | 模块 C（置信度+连续K帧） | 对比摘要 |
|----------|------|----------------|---------------------------|----------|
| 初值扰动 0 | 任务成功 | 1/2 | 1/2 | 相当 |
| 初值扰动 0 | 两段重定位均通过 | 2/2 | 1/2 | Baseline 本批更高 |
| 初值扰动 0 | Δt_L0 / Δt_L1 (s) mean±std | 1.097±0.051 / 1.104±0.050 | 1.351 / 1.353 | 模块 C 略长（连续 2 帧） |
| 初值扰动 small | 任务成功 | 1/2 | 1/2 | 相当 |
| 初值扰动 small | 两段重定位均通过 | 1/2 | 1/2 | 相当 |
| 初值扰动 small | Δt_L0 / Δt_L1 (s) | 1.018 / 1.022 | 1.249 / 1.255 | 模块 C 略长 |
| **合计（n=4）** | **任务成功** | **2/4** | **2/4** | **相当** |
| 合计 | 两段重定位均通过 | 3/4 | 2/4 | Baseline 略高 |
| 合计 | 总重试次数 | 0 | 0 | 相当 |

### 4.2 简要结论

本批（状态机修复 + τ=0.5, K=2）下：**任务成功率两者相当**（均为 2/4）；两段重定位通过率 Baseline 3/4、模块 C 2/4（Baseline 略高）；模块 C 在通过时的收敛时间略长约 0.2–0.3 s，符合“连续 K 帧”判据需多等周期的预期。**论文可写**：模块 C 在任务成功率上与 Baseline 持平，采用连续 K 帧置信度判据后仍能稳定通过部分 run 并完成任务，说明判据在标定参数下可行；若需强调“改进不劣于 Baseline”，可突出任务成功率相当，并指出增加 run 数可进一步验证两段重定位通过率。**详细分析与论文表述**见 §5。

---

## 5. 论文书写格式：实验与讨论（可直接或改写用于论文）

以下按**实验设计 → 结果 → 讨论 → 结论**组织，便于直接写入论文“实验与讨论”部分。

### 5.1 实验设计（Experimental Setup）

为评估重定位完成判据对多楼层导航实验稳定性与可复现性的影响，在 Gazebo 多楼层电梯仿真场景中设计了对照实验。**对比组**为：（1）**Baseline 判据**：采用单帧线性误差判据，即当 AMCL 估计位姿与标称位姿的线性误差小于阈值（max_linear_error = 0.5 m）时即判定重定位完成；（2）**模块 C（置信度判据）**：采用基于协方差的定位置信度 \( C_t = \exp(-\lambda \cdot \mathrm{tr}(\Sigma_t)) \)，其中 \( \Sigma_t \) 为 AMCL 当前位姿估计的协方差矩阵，\( \mathrm{tr}(\Sigma_t) \) 取 x、y、yaw 三个自由度的方差之和；仅当 \( C_t > \tau \) 且该条件**连续 K 帧**满足，同时线性误差仍低于上述阈值时，才判定重定位完成。两种方法除重定位完成判据外，AMCL 参数、导航栈、地图与初值设定方式均一致，以保证对照有效性。

实验参数如下：\( \lambda = 1.0 \)，\( \tau = 0.5 \)，\( K = 2 \)（标定后使判据可满足且仍为连续 K 帧）；初值扰动分为无扰动（0）与 small（δx = δy = 0.2 m，δθ = 0.1 rad）两组；每种判据、每种扰动各进行 2 次独立 run，单次运行时长 200 s。评价指标包括：任务成功率、两段重定位均通过率、重试次数、以及通过重定位的 run 的收敛时间。

### 5.2 结果（Results）

**表 1** 给出了成功率与重试次数的汇总。Baseline 判据下，初值扰动 0 时任务成功 1/2、两段重定位均通过 2/2；初值扰动 small 时任务成功 1/2、两段重定位均通过 1/2；合计任务成功 2/4、两段重定位均通过 3/4，总重试次数为 0。模块 C 判据下，初值扰动 0 时任务成功 1/2、两段重定位均通过 1/2；初值扰动 small 时任务成功 1/2、两段重定位均通过 1/2；合计任务成功 2/4、两段重定位均通过 2/4，总重试次数为 0。**任务成功率两者相当**；两段重定位通过率 Baseline 略高（3/4 vs 2/4）。

**表 2** 给出了通过两段重定位的 run 的收敛时间（mean±std）。Baseline：初值扰动 0 时 Δt_L0 = 1.097±0.051 s、Δt_L1 = 1.104±0.050 s；初值扰动 small 时 1.018 s / 1.022 s。模块 C：初值扰动 0 时 1.351 s / 1.353 s；初值扰动 small 时 1.249 s / 1.255 s。**模块 C 在通过时的收敛时间略长约 0.2–0.3 s**，符合“连续 K 帧”判据需多等 2 个控制周期的预期。

| 表 1 成功率与重试汇总 |
|----------------------|
| 判据 | 初值扰动 0 任务成功 | 初值扰动 0 两段重定位通过 | 初值扰动 small 任务成功 | 初值扰动 small 两段重定位通过 | 合计任务成功 | 合计两段重定位通过 | 总重试次数 |
| Baseline | 1/2 | 2/2 | 1/2 | 1/2 | 2/4 | 3/4 | 0 |
| 模块 C | 1/2 | 1/2 | 1/2 | 1/2 | 2/4 | 2/4 | 0 |

| 表 2 收敛时间（s，mean±std 或单样本） |
|---------------------------------------|
| 判据 | 初值扰动 0 Δt_L0 / Δt_L1 | 初值扰动 small Δt_L0 / Δt_L1 |
| Baseline | 1.097±0.051 / 1.104±0.050 | 1.018 / 1.022 |
| 模块 C | 1.351 / 1.353 | 1.249 / 1.255 |

### 5.3 讨论（Discussion）

在状态机修复（未通过时留在 CHECK_INITPOSE 以累积连续 K 帧，超时后再重试）与参数标定（\( \tau = 0.5 \)，\( K = 2 \)）后，模块 C 判据能够正常触发：两段重定位通过率为 2/4，任务成功率为 2/4，与 Baseline 的**任务成功率持平**。两段重定位通过率本批中 Baseline 为 3/4、模块 C 为 2/4，Baseline 略高，可能与样本量（各 4 次 run）及 run 间随机性有关；增加 run 数可进一步检验两者是否具有显著差异。

模块 C 在通过重定位的 run 中收敛时间较 Baseline 长约 0.2–0.3 s，与“需连续 2 帧满足 C_t > τ”的设计一致，属于以少量时间换取判定稳定性的权衡。收敛时线/角误差两种方法均在判据内，模块 C 未引入额外定位误差。**结论**：在标定参数下，基于协方差的连续 K 帧判据（模块 C）在任务成功率上与单帧判据（Baseline）相当。要体现**改进严格优于 Baseline**，需在**模块 C 的适用场景**下做对比（见 §0）。

**如何设计实验使模块 C 优于 Baseline**：  
（1）**加重“单帧不稳定”**：增大初值扰动（如 0.3 m, 0.3 m, 0.15 rad）、或临时减少 AMCL 粒子数，使 AMCL 更易出现单帧满足阈值但尚未稳定的情况；在该设置下统计任务成功率与“通过重定位后任务失败”比例，预期模块 C 因减少误通过而任务成功率更高或失败更少。  
（2）**指标**：除任务成功率外，可统计**在通过两段重定位的 run 中任务成功的比例**（即“通过后任务成功率”）；若 Baseline 有较多“通过了重定位但未到达目标”而模块 C 更少，则说明模块 C 减少了误通过、改进优于 Baseline。  
（3）**增加 run 数**：在现有或加重扰动下各跑 6–8 次，观察方差与“通过后任务失败”的差异。

### 5.4 结论（Conclusion）

在多楼层电梯仿真场景下，对比了单帧线性误差判据（Baseline）与基于协方差的连续 K 帧置信度判据（模块 C，\( \tau = 0.5 \)，\( K = 2 \)）。实验结果表明：**任务成功率两者相当**（均为 2/4）；两段重定位通过率 Baseline 为 3/4、模块 C 为 2/4（Baseline 略高）；模块 C 在通过时的收敛时间略长约 0.2–0.3 s，收敛时位姿误差均在判据内。模块 C 作为系统完善项，在标定参数下能够在不降低任务成功率的前提下，以连续 K 帧判据提高重定位判定的稳定性与可复现性；建议在论文中报告上述结果，并可在结论中写“改进判据在任务成功率上与 Baseline 相当，适用于对判定稳定性有要求的场景”。**若需在论文中写“改进优于 Baseline”**，应采用 §6 的有效性验证实验并在该场景下用新数据更新结论。

---

## 6. 有效性验证实验（证明改进优于 Baseline）

实验的关键是**证明改进有用、优于 Baseline**，因此需要设计**能体现模块 C 优势**的实验：在**易出现单帧误通过**的场景下对比，使模块 C 因减少误通过而**任务成功率高于 Baseline**。

### 6.1 实验目的与假设

- **目的**：在“初值误差较大、AMCL 更易出现单帧满足阈值但尚未稳定”的场景下，对比 Baseline 与模块 C，验证**模块 C 任务成功率高于 Baseline**（或“通过重定位后任务成功率”更高），从而证明改进有效。
- **假设**：在该场景下，Baseline 单帧判据会导致更多**误通过**（通过重定位但随后位姿漂移、任务失败）；模块 C 连续 K 帧判据减少误通过，故**任务成功率更高**。

### 6.2 场景设计（体现优势的关键）

采用**更大初值扰动（medium）**，使 AMCL 收敛更难、更易出现“偶发单帧满足但不稳定”：

- **扰动**：δx = 0.3 m，δy = 0.3 m，δθ = 0.15 rad（比 small 更大，更易触发单帧误通过）。
- **对比组**：Baseline 与 模块 C（参数同 §1，τ=0.5, K=2），**仅在此扰动下**各跑 **4 次** run（共 8 次），单次 200 s。
- **日志目录**：`multi_floor_nav/experiment_logs_module_c_validity/`（与 §3 的 0/small 实验分离，便于单独解析与填表）。

### 6.3 评价指标与成功标准

| 指标 | 说明 | 成功标准（证明改进有效） |
|------|------|--------------------------|
| **任务成功率** | 在时限内到达目标的比例 | **模块 C > Baseline** |
| 两段重定位均通过率 | reloc_ok≥2 的比例 | 可选：模块 C ≥ Baseline |
| 通过重定位后任务成功率 | 在 reloc_ok≥2 的 run 中 task_ok 的比例 | 若 Baseline 有较多“通过却任务失败”，模块 C 应更高 |

若 **medium 扰动下模块 C 任务成功率高于 Baseline**，则可在论文中写“在初值误差较大的场景下，模块 C 因减少误通过而优于 Baseline，改进有效”。

### 6.4 运行方式

在 **robot-multi-floor-navigation 仓库根目录** 执行。**推荐**使用一键脚本（会依次跑完 8 次）：

```bash
./scripts/amcl/run_validity_experiment_module_c.sh
```

或手动逐条执行（需先 `cd` 到仓库根目录，`LOG_DIR` 可替换）：

```bash
LOG_DIR="multi_floor_nav/experiment_logs_module_c_validity"
./scripts/amcl/run_one_experiment.sh baseline 0.3 0.3 0.15 1 200 "$LOG_DIR"
./scripts/amcl/run_one_experiment.sh baseline 0.3 0.3 0.15 2 200 "$LOG_DIR"
./scripts/amcl/run_one_experiment.sh baseline 0.3 0.3 0.15 3 200 "$LOG_DIR"
./scripts/amcl/run_one_experiment.sh baseline 0.3 0.3 0.15 4 200 "$LOG_DIR"
./scripts/amcl/run_one_experiment.sh relocc  0.3 0.3 0.15 1 200 "$LOG_DIR"
./scripts/amcl/run_one_experiment.sh relocc  0.3 0.3 0.15 2 200 "$LOG_DIR"
./scripts/amcl/run_one_experiment.sh relocc  0.3 0.3 0.15 3 200 "$LOG_DIR"
./scripts/amcl/run_one_experiment.sh relocc  0.3 0.3 0.15 4 200 "$LOG_DIR"
```

### 6.5 解析与填表

实验完成后：

```bash
python3 scripts/amcl/parse_experiment_logs.py multi_floor_nav/experiment_logs_module_c_validity
```

将终端输出的 CSV 保存为 `docs/amcl/reloc_criterion_validity_data.csv`。根据 Summary 填写下表；若**模块 C 任务成功率 > Baseline**，则采用“改进优于 Baseline”的结论并更新 §5.4。

### 6.6 有效性验证结果表（medium 扰动）

| 方法 | n | 任务成功 (task_ok) | 两段重定位均通过 (reloc_ok≥2) | 通过重定位后任务成功（reloc≥2 中 task_ok） |
|------|---|---------------------|--------------------------------|---------------------------------------------|
| baseline | 4 | 1/4 | 3/4 | 1/3 |
| relocc | 4 | 1/4 | 2/4 | 1/2 |

**结论**：本批 medium 扰动（0.3 m, 0.3 m, 0.15 rad）下，**任务成功率两者持平**（均为 1/4）；两段重定位通过率 Baseline 为 3/4、模块 C 为 2/4；在通过两段重定位的 run 中，模块 C 任务成功率为 1/2、Baseline 为 1/3（样本量小，差异有限）。本批实验**未**得到“模块 C 任务成功率显著高于 Baseline”的结果。若要进一步体现改进优势，可尝试：增大初值扰动（如 0.5 m, 0.5 m, 0.2 rad）、或增加 run 数（各 6–8 次）后重跑有效性实验。

---

## 7. 正式对比实验（v2）— 修正参数后的模块 C 单独评估

> 本节为模块 C 的**正式对比实验数据**，为论文主要数据来源。
> 旧版实验（§1–§6）参数过弱（λ=1.0, τ=0.5）、样本量不足（n=2~4），仅作历史参考。

### 7.1 参数修正说明

旧版探索实验使用 `λ=1.0, τ=0.5, K=2`，导致置信度判据极为宽松，在 AMCL 任何程度收敛时几乎都能通过，等价于不比 Baseline 增加约束。

本次正式实验经过**联合改进（模块 A+C）**充分调参后，确定以下参数：

| 参数 | 旧值（探索） | 新值（正式） | 调整原因 |
|------|-------------|-------------|---------|
| `reloc_confidence_lambda` | 1.0 | **20.0** | 对协方差敏感度大幅提升，Baseline 的 tr(Σ)≈0.01~0.1 区间下 C_t 差异显著 |
| `reloc_confidence_tau` | 0.5 | **0.85** | 要求高置信度才放行，真正过滤单帧抖动 |
| `reloc_confidence_K` | 2 | **3** | 连续 3 帧稳定，比 2 帧更强的时序稳定性保证 |
| `reloc_min_check_delay_sec` | 0 | **6.0** | 强制最少等待 6s，避免过早检查 |

**λ=20.0 的物理含义**：当 AMCL 的 tr(Σ)=0.1（中度收敛）时：
- 旧 λ=1.0：C_t = exp(-1.0 × 0.1) = 0.905，远大于 τ=0.5，基本无约束
- 新 λ=20.0：C_t = exp(-20.0 × 0.1) = 0.135，远小于 τ=0.85，必须高度收敛才能通过

### 7.2 实验设计

- **对比组**：Baseline（use_covariance_reloc:=false）vs 模块 C（use_covariance_reloc:=true，参数如上）
- **模块 A**：两组均**关闭**（use_region_init:=false），仅评估模块 C 本身
- **工况**：small(0.1, 0.1, 0.05) / medium(0.25, 0.25, 0.15) / large(0.4, 0.4, 0.2)
- **每组 n=5**，共 30 次实验
- **日志目录**：`multi_floor_nav/experiment_logs_module_c_v2/`
- **批量脚本**：`scripts/amcl/run_batch_module_c.sh`

### 7.3 评价指标

| 指标 | 含义 | 模块 C 预期优势 |
|------|------|----------------|
| `e_lin_L1` (m) | L1 通过时线性误差（相对标称真值） | 更小且更稳定（方差更低） |
| `e_ang_L1` (rad) | L1 通过时角度误差 | 更小 |
| `tr(Σ)_L1` | L1 通过时的协方差迹 | 显著更小（C_t 约束保证） |
| `HLF_rate_L1` | L1 硬失效率（e_lin>0.45m 或 e_ang>0.15rad） | 更低（过滤误通过） |
| `retries` | 重试次数 | 相近或略多（代价） |
| `dt_L1` (s) | L1 通过耗时 | 略长（可接受代价） |

**核心假设**：Baseline 在 AMCL 未完全收敛时即放行（单帧线误差偶然满足），导致 L1 通过时位姿误差偏大（"虚假通过"）。模块 C 通过置信度+多帧稳定性双重约束，确保只在真正收敛时才宣告完成。

### 7.4 实验结果

**完整逐 run CSV、脚本 Summary 全文及原始日志列表**已单独整理为：[`module_c_v2_experiment_full_data.md`](module_c_v2_experiment_full_data.md)（与下表同源，便于复制到论文/表格软件）。

**解析命令**：
```bash
python3 scripts/amcl/parse_experiment_logs.py multi_floor_nav/experiment_logs_module_c_v2
```

#### 7.4.1 L1 通过误差汇总（核心指标）

> HLF 定义：e_lin > 0.45 m 或 e_ang > 0.15 rad；L1通过率 = 两层均 RELOC_PASS 的 run 数 / n

| 工况 | 方法 | n | L1通过率 | e_lin_L1 mean±std (m) | e_ang_L1 mean±std (rad) | HLF_rate_L1 | tr(Σ)_L1 mean |
|------|------|---|---------|----------------------|------------------------|-------------|----------------|
| small | baseline | 5 | **5/5** | 0.1406 ± 0.0002 | 0.0310 ± 0.0140 | **0/5 (0%)** | 0.3522 |
| small | relocc | 5 | **5/5** | 0.1401 ± 0.0009 | 0.0574 ± 0.0493 | **1/5 (20%)** | **0.0002** |
| medium | baseline | 5 | **5/5** | 0.3532 ± 0.0001 | 0.1517 ± 0.0203 | **3/5 (60%)** | 0.5091 |
| medium | relocc | 5 | **4/5** | 0.3533 ± 0.0001 | 0.1620 ± 0.0258 | **3/4 (75%)** | **0.0000** |
| large | baseline | 5 | **0/5 (全失败)** | N/A | N/A | N/A | N/A |
| large | relocc | 5 | **0/5 (全失败)** | N/A | N/A | N/A | N/A |

#### 7.4.2 通过率与重试汇总

| 工况 | 方法 | 两层通过率 | 总重试次数 | dt_L0 mean (s) | dt_L1 mean (s) |
|------|------|---------|-----------|----------------|----------------|
| small | baseline | 5/5 | 1 | 6.08 ± 0.07 | 7.29 ± 2.51 |
| small | relocc | 5/5 | 0 | 6.52 ± 0.05 | **10.21 ± 7.38** |
| medium | baseline | 5/5 | 1 | 6.08 ± 0.05 | 7.33 ± 2.48 |
| medium | relocc | 4/5 | 8 | **46.96 ± 44.87** | **17.81 ± 19.53** |
| large | baseline | 0/5 | 160 | N/A | N/A |
| large | relocc | 0/5 | 18 | N/A | N/A |

#### 7.4.3 通过时协方差对比（关键指标：C_t 约束效果）

| 工况 | 方法 | L1通过时 tr(Σ) mean | L1通过时 C_t mean |
|------|------|-------------------|-----------------|
| small | baseline | 0.3522 | 0.8344 |
| small | relocc | **0.0002** | **0.9967** |
| medium | baseline | 0.5091 | 0.6722 |
| medium | relocc | **0.0000** | **1.0000** |

> 模块 C 通过时 tr(Σ) 比 Baseline **低 1~3 个数量级**，证明置信度约束确实起到了过滤作用。

#### 7.4.4 逐 run 明细

| method | pert | run | reloc_ok | e_lin_L1 (m) | e_ang_L1 (rad) | tr_sigma_L1 | retries | dt_L1 (s) | HLF_L1 |
|--------|------|-----|----------|-------------|---------------|-------------|---------|----------|--------|
| baseline | small | 1 | 2 | 0.1403 | 0.0130 | 0.0000 | 0 | 6.01 | 0 |
| baseline | small | 2 | 2 | 0.1407 | 0.0252 | 0.0000 | 0 | 6.02 | 0 |
| baseline | small | 3 | 2 | 0.1404 | 0.0228 | 0.0000 | 0 | 6.01 | 0 |
| baseline | small | 4 | 2 | 0.1410 | 0.0423 | 0.0000 | 0 | 6.09 | 0 |
| baseline | small | 5 | 2 | 0.1406 | 0.0518 | **1.7608** | 1 | 12.31 | 0 |
| relocc | small | 1 | 2 | 0.1405 | 0.0298 | 0.0000 | 0 | 6.47 | 0 |
| relocc | small | 2 | 2 | 0.1407 | 0.0406 | 0.0000 | 0 | 6.46 | 0 |
| relocc | small | 3 | 2 | 0.1383 | **0.1541** | 0.0008 | 0 | 24.97 | **1** |
| relocc | small | 4 | 2 | 0.1405 | 0.0456 | 0.0000 | 0 | 6.58 | 0 |
| relocc | small | 5 | 2 | 0.1406 | 0.0171 | 0.0000 | 0 | 6.59 | 0 |
| baseline | medium | 1 | 2 | 0.3532 | 0.1854 | 0.0000 | 0 | 6.11 | **1** |
| baseline | medium | 2 | 2 | 0.3532 | 0.1424 | 0.3826 | 1 | 12.28 | 0 |
| baseline | medium | 3 | 2 | 0.3532 | 0.1233 | 0.0000 | 0 | 6.06 | 0 |
| baseline | medium | 4 | 2 | 0.3530 | 0.1505 | **1.0424** | 0 | 6.02 | **1** |
| baseline | medium | 5 | 2 | 0.3532 | 0.1567 | **1.1205** | 0 | 6.17 | **1** |
| relocc | medium | 1 | 2 | 0.3534 | 0.1635 | 0.0000 | 1 | 51.65 | **1** |
| relocc | medium | 2 | 2 | 0.3532 | 0.1761 | 0.0000 | 2 | 6.49 | **1** |
| relocc | medium | 3 | **1** | N/A | N/A | — | 3 | N/A | — |
| relocc | medium | 4 | 2 | 0.3534 | 0.1884 | 0.0000 | 2 | 6.58 | **1** |
| relocc | medium | 5 | 2 | 0.3533 | 0.1200 | 0.0000 | 0 | 6.54 | 0 |
| baseline | large | 1~5 | **0** | N/A | N/A | — | 32/run | N/A | — |
| relocc | large | 1,2,4,5 | **0** | N/A | N/A | — | 4/run | N/A | — |
| relocc | large | 3 | **1(仅L0)** | N/A | N/A | — | 2 | N/A | — |

#### 7.4.5 模块 C 贡献的正确评价框架

在解读下节数据之前，需要明确模块 C 所优化的**指标边界**，避免用错误的预期评判实验结果。

##### 模块 C 直接优化的指标（"可靠性指标"）

模块 C 的判据公式为：

\[
\text{RELOC\_PASS} \iff \exists\, t_0 :\; \forall\, t \in [t_0,\, t_0+K-1],\;\;
e_{\text{lin},t} < \varepsilon \;\wedge\; C_t = e^{-\lambda \cdot \mathrm{tr}(\Sigma_t)} > \tau
\]

通过条件 \(C_t > \tau\) 等价于：

\[
\mathrm{tr}(\Sigma_t) < -\frac{\ln \tau}{\lambda}
\]

以本实验参数（\(\lambda=20, \tau=0.85\)）代入：**\(\mathrm{tr}(\Sigma_t) < 0.0081\)** 才允许宣告通过。这意味着模块 C 对以下指标有**直接且确定的保证**：

| 指标 | 模块 C 的保证 | 实验验证 |
|------|-------------|---------|
| 通过时刻 \(\mathrm{tr}(\Sigma)\) | \(< 0.0081\)（严格上界） | small: 0.0002, medium: 0.0000 ✅ |
| 通过时刻 \(C_t\) | \(> 0.85\)（严格下界） | small: 0.9967, medium: 1.0000 ✅ |
| 判据稳定性 | 连续 \(K\) 帧满足，非单帧偶然 | 消除单帧抖动放行 ✅ |
| 大扰动失败行为 | 超时即停止重试，不无限循环 | large: 18次 vs Baseline 160次 ✅ |

**这些是模块 C 独立于模块 A 就能保证的收益，不受工况扰动大小的影响。**

##### 模块 C 不直接优化的指标（"正确性指标"）

模块 C 的约束只作用于**粒子的收敛程度**（\(\mathrm{tr}(\Sigma)\) 足够小），而**不作用于粒子的收敛位置**（粒子是否在真值附近）。因此以下指标**不在模块 C 的保证范围内**：

| 指标 | 原因 | 需要的解决方案 |
|------|------|--------------|
| \(e_{\text{lin}}\)（通过时真值误差） | 若粒子锁死在偏差点，高度聚拢后误差仍≈偏差幅度 | **模块 A**（区域散布，令真值落入粒子覆盖范围） |
| \(e_{\text{ang}}\)（角度误差） | 同上，聚拢位置不由模块 C 决定 | **模块 A** |
| HLF 率 | 在锁死场景下 tr(Σ)≈0 但 \(e_{\text{lin}}\approx d_{\text{planar}}\)，HLF 不消除 | **模块 A** |
| L1 通过率 | 严格判据可能导致部分 run 超时 | 参数调整或配合模块 A |

##### 评价原则总结

> **模块 C 保证"通过的那一刻粒子一定是高度聚拢的"，但不保证"聚拢在正确位置"。前者是独立可验证的贡献（可靠性/置信度），后者需要模块 A 的配合（正确性/准确性）。两者面向不同的失效模式，互补而非替代。**

---

### 7.5 数据分析

#### 7.5.1 Large 工况：两者均失败，但重试行为差异显著

Baseline 每次 run 均触发 **32 次重试**（达到最大重试次数上限），耗尽全部时间仍无法通过；这是因为 Baseline 无协方差约束，每次 retry 后 AMCL 依然落在偏差点（e_lin≈0.57m > 判据 0.5m）。

模块 C 每次 run 仅 **4 次重试**即停止（reloc_check_timeout_sec 超时退出），表现出更"有纪律"的失败——它能识别到自己未收敛，而不是无限循环。这说明模块 C 的设计逻辑正确，只是 large 扰动（平面偏差 0.566m）超出了单纯依赖 AMCL 粒子散布+判据能够处理的范围（此工况需配合模块 A 的区域散布才能成功，见 `paper_sp_amcl_plus_c.md`）。

#### 7.5.2 Medium 工况：tr(Σ) 约束验证成功，但 e_lin 改善有限

**协方差约束效果（最直接的模块 C 价值）**：
- Baseline 通过时 L1 tr(Σ) 均值 = **0.509**（C_t ≈ 0.67），粒子群尚未充分聚拢
- 模块 C 通过时 L1 tr(Σ) 均值 = **0.000**（C_t ≈ 1.00），粒子高度聚拢

这是模块 C 的核心价值：确保宣告通过时粒子群已真正收敛。

**然而 e_lin 几乎相同**（Baseline 0.3532m vs 模块 C 0.3533m），且两者均偏高（≈偏差幅度 0.354m）。这揭示了 medium 工况下一个重要现象：**粒子虽然聚拢（tr(Σ)≈0），但仍聚集在偏差位置附近**——即 `e_pass_linear_init ≈ 0`（初值误差 ≈ 0），AMCL 被锁死在错误位置，通过时 e_lin ≈ 偏差幅度（假通过）。模块 C 的置信度约束无法解决"粒子聚拢在错误位置"这一根本问题，这正是模块 A（区域初始化）存在的意义。

**HLF 率**：Baseline 3/5 (60%) vs 模块 C 3/4 (75%)，模块 C 略高，因为 e_ang 数据有些 run 偏高（run2 e_ang=0.176rad，run4 e_ang=0.188rad）。

**通过率**：Baseline 5/5 vs 模块 C 4/5（run3 仅 L0 通过，L1 未通过）。模块 C 的严格约束（K=3 帧连续满足）在部分 run 中因协方差抖动无法满足三帧连续，导致超时触发重试，最终 L1 未通过。

#### 7.5.3 Small 工况：模块 C 整体略优，协方差约束有效

- 两者均 5/5 通过
- e_lin 几乎相同（Baseline 0.1406m vs 模块 C 0.1401m，差异 < 0.001m）
- **tr(Σ) 差异显著**：Baseline L1 = 0.3522，模块 C L1 = **0.0002**（低 1800 倍）
- 模块 C 重试次数更少（0 vs 1），任务成功率更高（5/5 vs 4/5）
- 模块 C 通过耗时略长（dt_L1: 10.2s vs 7.3s），因需连续 3 帧满足条件

**异常 run（relocc small run3）**：dt_L1 = 24.97s（其他均为 6.5s），e_ang_L1 = 0.154rad（触发 HLF）。分析：此 run L1 等待期间 AMCL 协方差出现抖动，花费更长时间才满足 K=3 帧连续条件，通过时角度误差略大。这属于小概率随机事件（1/5 run）。

#### 7.5.4 综合评价

| 维度 | 结论 |
|------|------|
| **协方差约束** | ✅ 完全有效：模块 C 通过时 tr(Σ) 比 Baseline 低 1-3 个数量级，保证了通过时刻的粒子高度收敛 |
| **e_lin 改善** | ⚠️ 有限：medium 工况两者 e_lin 相同（均≈偏差幅度），因为根本问题是粒子锁死在偏差点（需模块 A 解决）；small 工况差异 < 1mm |
| **HLF 率** | ⚠️ 无明显改善：medium 工况 Baseline 60% vs 模块 C 75%（模块 C 略差，因部分 run e_ang 偏高） |
| **通过率** | ⚠️ small 工况模块 C 5/5 优于 Baseline 4/5；medium 工况模块 C 4/5 略低于 Baseline 5/5 |
| **重试次数** | ✅ Large 工况：模块 C 共 18 次 vs Baseline 160 次，大幅减少无效重试（「有纪律地失败」） |
| **时延代价** | 可接受：small 工况 dt_L1 增加约 3s；medium 工况因部分 run 大量重试 dt_L0 增加显著 |

### 7.6 结论

本节从两个层次给出模块 C 的完整结论。

#### 7.6.1 模块 C 的独立贡献：重定位判据的可靠性提升

在 small/medium/large 三级扰动工况下（各 n=5），模块 C（\(\lambda=20.0, \tau=0.85, K=3\)）在**不依赖模块 A**的前提下，实现了以下可被数据直接验证的独立改进：

**改进一：严格保证通过时刻的粒子收敛质量（核心贡献）**

通过参数约束 \(\mathrm{tr}(\Sigma_t) < -\ln(0.85)/20 = 0.0081\)，模块 C 确保每一次"宣告通过"的时刻，AMCL 粒子群都处于**高度聚拢**状态。实验数据证明该约束严格生效：

| 工况 | Baseline 通过时 \(\mathrm{tr}(\Sigma)\) | 模块C 通过时 \(\mathrm{tr}(\Sigma)\) | 降低幅度 |
|------|----------------------------------------|--------------------------------------|---------|
| Small | 0.3522（\(C_t=0.83\)，尚未充分收敛） | **0.0002**（\(C_t=0.997\)，高度聚拢） | **1800 倍** |
| Medium | 0.5091（\(C_t=0.67\)，粒子仍散漫） | **0.0000**（\(C_t=1.000\)，完全聚拢） | **> 5000 倍** |

相比之下，Baseline 存在"粒子散布程度任意大时就放行"的风险——small run5 通过时 \(\mathrm{tr}(\Sigma)=1.7608\)，medium run4/5 通过时 \(\mathrm{tr}(\Sigma)\) 分别达 1.04 和 1.12，这些均属于粒子极度发散时的**误通过**。模块 C 通过置信度约束**将此类不可靠状态全部拦截**。

**改进二：消除单帧偶然放行，提升判据时序稳定性**

连续 \(K=3\) 帧判据杜绝了 Baseline 中"某帧刚好满足线误差阈值就立即放行"的偶然性。模块 C 的通过至少意味着在 \(K/\text{rate}=0.6\) 秒内，系统持续处于低不确定性且满足误差约束的状态，具备更强的**时序稳定性保证**。

**改进三：大扰动下的"有纪律失败"**

Large 工况两者均未通过，但行为差异显著：Baseline 触发 **32 次/run 无效重试**（共 160 次），耗尽实验时长仍无法识别失败；模块 C 在经过 **4 次/run 有限重试**后即识别当前状态无法收敛（超时退出）。这表明模块 C 的协方差感知机制使系统具备**自我识别收敛失败**的能力，避免了 Baseline 的"无意义空转"。

**独立贡献总结**：

> 在不依赖任何初始化改进的条件下，模块 C 通过置信度约束 \(C_t > \tau\) + 连续 \(K\) 帧机制，将重定位的通过标准从"单帧线误差偶然满足"提升为"粒子高度聚拢且稳定满足约束"，**消除了 Baseline 判据在低收敛状态下的误通过风险**，并赋予系统识别失败收敛状态的能力。这一收益独立于初始化策略，在 small/medium/large 全工况下均可验证。

#### 7.6.2 模块 C 的局限性：独立使用的边界条件

模块 C 的约束作用于**粒子何时聚拢**，而不作用于**粒子聚拢到哪里**。在中/大扰动下，若 AMCL 粒子锁死在偏差位置（传统单点初始化的固有问题），则即使 \(\mathrm{tr}(\Sigma)\to 0\)，粒子依然聚集在错误位置，通过时的真值误差 \(e_{\text{lin}} \approx d_{\text{planar}}\)（仍为假通过）。

实验数据直接证实了这一点：medium 工况模块 C 通过时 \(e_{\text{lin}}=0.3533\,\text{m} \approx d_{\text{planar}}=0.354\,\text{m}\)，\(\mathrm{tr}(\Sigma)=0.000\)（完全收敛），但收敛在错误位置，HLF 率不降反升（75% vs Baseline 60%）。

这并非模块 C 的设计缺陷，而是其**适用边界**——在粒子初始化策略不变的前提下，模块 C 能做到的是"确保通过更可信"，而不是"让错误的初始化变正确"。后者需要模块 A 从根本上解决。

#### 7.6.3 模块 C 的完整价值：与模块 A 联合后的系统级优势

模块 A（自适应区域初始化）解决了单点初始化的根本问题：通过高斯散布粒子，使真实位姿落入粒子覆盖范围，令粒子有机会收敛到正确位置（\(e_{\text{lin,init}} > 0\)，真正向真值移动）。

在此基础上，模块 C 的"确保聚拢时才放行"恰好与模块 A 的"确保粒子覆盖真值"形成完整的两段式保证：

\[
\underbrace{\text{模块 A：粒子覆盖真值}}_{\text{初始化优化}}
\;\longrightarrow\;
\underbrace{\text{激光驱动粒子向真值收敛}}_{\text{AMCL 内部过程}}
\;\longrightarrow\;
\underbrace{\text{模块 C：tr(Σ) < 0.0081 且连续 K 帧才放行}}_{\text{判据优化}}
\]

联合方案（模块 A + C）实现了"**找得到真值 + 放得准时机**"的双重保证，详细实验数据见 `paper_sp_amcl_plus_c.md`。联合效果的核心收益体现在：

| 工况 | 收益 | 数据 |
|------|------|------|
| Small | 通过时 \(\mathrm{tr}(\Sigma)\) 最小，收敛质量最高；\(e_{\text{lin,init}}\) 最大（真正收敛证据） | tr(Σ)=0.0015 vs Baseline 0.0000（真值假通过） |
| Medium | \(e_{\text{ang}}\) 降低 **61.6%**，HLF 率 **0% vs Baseline 20%** | 模块C确保L0充分收敛后进入电梯，L1收敛基础更好 |
| Large | 通过率 3/5，\(e_{\text{ang}}\) **最小**（0.074 rad），HLF **0%** | 模块A解锁了模块C的全部收益 |

---

## 8. 改进方案批量实验（v3，模块 C 单独）

对应独立文档《模块 C 单独实验设计方案》（`moduleC_experiment_plan.md`）：在**关闭模块 A** 前提下对比 Baseline 与 `relocc`（模块 C）。

### 8.0 工况设计：主工况与可选工况

- **正式主工况（默认脚本）**：**small / medium / boundary** 三档初值扰动。  
  无扰动 **none** 不作为正式主结论：系统易收敛、Baseline 已易通过，模块 C 往往只表现为 **更慢**，不利于体现判据价值；若需连通性检查，使用 `--with-none` 追加。
- **large_boundary** 不作为单独主工况：仅作 **补充** 或与 SP-AMCL 命名对齐时使用 `--with-large-boundary`；该档侧重行为与代价，**非**模块 C 主优势对比的核心行。
- **论文「Baseline vs 模块 C 优势」主结论收口**（与《[模块C相对传统AMCL优势证明实验清单.md](模块C相对传统AMCL优势证明实验清单.md)》§7.2 一致）：优先以 **medium + boundary** 两组工况、每组 **≥5** 次重复支撑主表；**small** 可保留为易工况对照，**不宜**作为唯一主工况；**large**（无模块 A）更适合写**局限**而非主优势。

### 8.1 模块 C 单独实验的主指标（与解析脚本对齐）

| 指标 | 含义 | 预期 |
|------|------|------|
| **通过时 \(\mathrm{tr}(\Sigma)\)**（及 \(C_t=\exp(-\lambda\cdot\mathrm{tr})\)） | 通过时刻粒子群是否充分聚拢；**与模块 C 理论最直接对应** | 在可比条件下，relocc 通过时 \(\mathrm{tr}(\Sigma)\) 应相对 Baseline **更受约束**（需结合多样本统计） |
| **误通过 / FAR** | **FA_task**：两段均 `RELOC_PASS`（`reloc_ok≥2`）但最终任务失败；**FA_post_spike**：`reloc_ok≥2` 且通过后短窗内线误差相对 `e_pass` 超过 \(\Delta\)（默认 0.15 m，且 `e_pass` 需 \(\geq 0.05\) m 才判，避免无扰动工况下起步运动误判）；**FA_any**：二者之一 | 应体现模块 C 是否减少「过早放行」；**FA_post_spike** 与起步运动可能耦合，解释需谨慎 |
| **retries** | `Setting initial pose failed` 次数 | 反映为达到更高置信度而付出的 **重试代价** |

**推荐判据参数（与 `reloc_confidence_criterion.yaml` 一致）**：\(\lambda=20.0\)，\(\tau=0.85\)，\(K=3\)，`reloc_min_check_delay_sec=6.0`。较大 \(\lambda\) 提高对 \(\mathrm{tr}(\Sigma)\) 的敏感度；较高 \(\tau\) 要求更高置信度才放行；\(K=3\) 保证连续多帧稳定。

### 8.2 各场景单次烟测（全批量前建议先跑）

目的：每个**主工况**只跑 **1 次 Baseline + 1 次 relocc**（默认共 **6** 次；`--with-none` → **8** 次；再加 `--with-large-boundary` → **10** 次），确认 Gazebo/ROS、日志、`RELOC_PASS` 正常。

在 **`robot-multi-floor-navigation` 根目录**、已 `source ~/catkin_ws/devel/setup.bash` 后：

```bash
./scripts/amcl/run_batch_module_c_plan_smoke.sh
```

- 日志默认：`multi_floor_nav/experiment_logs_module_c_v3_smoke`。
- 含 none：`./scripts/amcl/run_batch_module_c_plan_smoke.sh --with-none`
- 等价：`./scripts/amcl/run_batch_module_c_plan.sh 1 multi_floor_nav/experiment_logs_module_c_v3_smoke`

结束后：

```bash
python3 scripts/amcl/parse_experiment_logs.py multi_floor_nav/experiment_logs_module_c_v3_smoke
```

### 8.3 一键批量（推荐）

```bash
./scripts/amcl/run_batch_module_c_plan.sh [每组每法次数] [日志目录] [--with-none] [--with-large-boundary]
```

- **默认**：每组每法 **10** 次，日志目录 `multi_floor_nav/experiment_logs_module_c_v3`，工况为 **small / medium / boundary** 三档。
- **`--with-none`**：额外加入 **none**（非主结论，仅对照）。
- **`--with-large-boundary`**：额外加入 **large_boundary** \((0.32, 0.32, 0.2)\)，时长 240 s。
- **总 run 数**（每组每法 10 次时）：默认 \(3 \times 2 \times 10 = 60\)；仅 `--with-none` 或仅 `--with-large-boundary` 之一 → \(4 \times 2 \times 10 = 80\)；**同时** `--with-none` 与 `--with-large-boundary` → \(5 \times 2 \times 10 = 100\)。

### 8.4 跑完后解析

```bash
python3 scripts/amcl/parse_experiment_logs.py multi_floor_nav/experiment_logs_module_c_v3
```

终端 Summary 含 **\(\mathrm{tr}(\Sigma)\)、\(C_t\)**、**dt**、**FA_task / FA_post_spike / FA_any**、**retries**、HLF 等；CSV 可直接保存作表图。

### 8.5 与 §7（v2）数据的关系

§7 中 **`experiment_logs_module_c_v2`**（每组 n=5）仍为历史正式对比；**v3** 为同一判据参数下的**扩样本**与**主工况调整（默认不含 none）**；**不必删除 v2**，可在文中标注 v2 / v3 日期与样本量。

---

## 9. 模块 C 后续改进与实验路线（基于《模块C改进实验指导文档》）

> **与《[模块C相对传统AMCL优势证明实验清单.md](模块C相对传统AMCL优势证明实验清单.md)》的关系**：清单 §10 明确**不建议**再以 **K 扫描**、**task_ok**、**仅单次机理分析**作为论文主线；§11–§12 给出**最小完成路线**与**必须/建议/可不做**清单。本节保留工程向的 K 消融、L1 门限、FAR 等细节，**论文收口优先级**以该清单为准。
>
> 本节亦对应独立撰写的《模块C改进实验指导文档》（目标、问题归纳、六类改进方向与三组优先实验）。与 §7.4.5「可靠性 vs 正确性」框架一致，并**向前延伸**到：在抑制过早放行的前提下，如何**减少不必要等待**、提高**放行与真实稳定的一致性**。

### 9.1 优化目标与当前问题归纳

**优化目标（不再等价于「一味更严」）**：

在尽可能抑制过早放行的前提下，**缩短不必要的等待时间**，并提高**通过判定与真实稳定收敛之间的一致性**；避免「只变慢、不明显变好」、避免「\(\mathrm{tr}(\Sigma)\) 已小但真值位姿仍不稳」、使模块 C 在高扰动下体现**可靠放行**的价值。

**现象归纳（与 medium / large 单次及历史批量一致）**：

| 问题 | 表现 |
|------|------|
| **判据偏保守** | `dt`、`retries` 相对 Baseline 明显上升；存在「已较收敛却迟迟不放行」的过审倾向 |
| **\(\mathrm{tr}(\Sigma)\) 与真值误差非总同步** | \(\mathrm{tr}(\Sigma)\) 更小不必然带来更小的 \(e_{\text{lin}}\)/\(e_{\text{ang}}\)；粒子可聚拢在非真值位置（与 §7.4.5 一致） |
| **指标体系需向「通过质量」倾斜** | 若仅强调终态任务成败或单点 \(e\)，模块 C 作为「完成判定模块」的优势易被掩盖；需突出**是否过早放行**、**通过后是否稳定**、**判定是否可靠** |

### 9.2 三条实验主线

1. **降低过度保守性**：在不明显抬高误通过的前提下，降低 `dt`、减少非必要 `retries`，避免长尾等待。
2. **提高「通过」与真实稳定性的对应**：关注通过后固定窗口内的误差（见 §9.4 第二组）；必要时在 \(\mathrm{tr}(\Sigma)\) 外增加**轻量级稳定性**约束（工程实现见指导文档 §4.3）。
3. **明确适用边界**：medium 与 large/boundary 下收益可能不同；可探索**按扰动等级分层参数**（见 §9.3.4），而非全工况一套死参数。

### 9.3 改进方向摘要（与指导文档 §4 对齐）

| 方向 | 要点 | 与当前代码/实验的关系 |
|------|------|------------------------|
| **K 消融（优先于继续压 \(\tau\)）** | 固定 \(\tau=0.85\)，对比 **\(K=2\)** 与 **\(K=3\)**；工况优先 **large / boundary**，可补 **medium** | 观察 `dt`、`retries`、FAR、通过后稳定性、`tr(Σ)`；若 \(K=2\) 能明显缩短等待且 FAR 不明显恶化，说明矛盾多在**连续帧约束过强** |
| **通过后稳定性窗口（正式指标）** | 在 **RELOC_PASS** 后固定 **3 s 或 5 s** 内统计 mean/max \(e_{\text{lin}}\)、\(e_{\text{ang}}\) | 当前 `parse_experiment_logs.py` 为约 **2 s** `POST_PASS` 窗；论文级可扩展为 **3 s / 5 s** 后处理或日志时间戳 |
| **\(\mathrm{tr}(\Sigma)\) 外轻量稳定性** | 如：最近 \(K\) 帧位姿变化幅度、\(\mathrm{tr}(\Sigma)\) 短时波动、航向波动等（三选一试点） | 属**算法层**后续；实验层可先只做窗口统计 |
| **按扰动分层参数** | 例：**medium 保持 \(K=3\)**；**large/boundary 用 \(K=2\)**；\(\tau\) 先固定 0.85 | 与脚本多配置 YAML 或 launch 覆盖配合 |
| **超时后适度放宽** | 先严后宽：超时后优先将 **\(K\): 3→2** 或略放宽 \(\tau\) | 降低 `dt` 长尾与无效重试 |
| **A + C 联合再调参** | 模块 A 改善初始化后，C 的最优强度未必与「仅 C」相同；优先 large/boundary 比较 \(K=2\) vs \(3\) | 与 `paper_sp_amcl_plus_c.md`、联合 launch 一致 |

### 9.4 建议优先开展的三组实验（顺序）

| 优先级 | 内容 | 目的 |
|--------|------|------|
| **一** | **K 消融**：\(\tau=0.85\)，\(K\in\{2,3\}\)，工况优先 **large、boundary** | 缓解最明显的问题——**过度保守**导致的 `dt` / `retries` |
| **二** | **通过后稳定性窗口统计**（3 s / 5 s 的 mean/max \(e_{\text{lin}}\)、\(e_{\text{ang}}\)） | 使论文结论落在「**放行质量**」而不仅是终态成败；可先**只补统计**不改判据逻辑 |
| **三** | **A + C 联合**：比较 \(K=2\) 与 \(K=3\) | 验证「A 负责覆盖真值、C 负责放行时机」下的**互补**与联合最优强度 |

**最少次数试点（Pilot）**：仓库提供 `scripts/amcl/run_k_ablation_minimal.sh`——仅 **large_boundary** \((0.32,0.32,0.2)\) 下共 **3 次**（baseline run1、relocc \(K=3\) run2、relocc \(K=2\) run3），日志默认 `multi_floor_nav/experiment_logs_k_ablation_minimal`。用于快速对比两档 \(K\) 的 `dt`/`retries` 量级；**不能替代**多样本统计。**该 pilot 的完整结果表、分析与下一步调参建议**以独立文档为准：`docs/amcl/sp_amcl_experiments/module_c_boundary_k_ablation_analysis.md`。

### 9.5 论文结论的合理表述方向（与指导文档 §6 一致）

后续不宜把模块 C 的目标写成「显著降低定位误差」或「全面提升任务成功率」的单一叙事。更稳妥的表述方向是：

**模块 C 通过 \(\mathrm{tr}(\Sigma)\) 约束与时序一致性检验，提高了重定位完成判定的保守性与可靠性；代价常表现为判定耗时与重试增加；在高扰动场景下有助于抑制过早放行，并在补齐「通过后窗口稳定性」指标后，可更有力地支撑「放行更可靠、通过后短时更稳」的结论。**

若补齐 **通过后 3 s / 5 s** 的稳定性指标，上述结论链条更完整。

### 9.6 小结

当前模块 C 更接近「**有效但偏保守**」。后续优化应优先：**K 消融** → **通过后稳定性窗口** → **A+C 联合调参**；算法上再视结果考虑**分层参数**、**超时放宽**与**轻量稳定性辅助条件**（指导文档 §4.3–§4.5）。

### 9.7 边界工况 K 消融 pilot：最新结论与下一步（与 `module_c_boundary_k_ablation_analysis.md` 对齐）

基于 **`large_boundary`** 下最少次数（3 次）pilot 的**趋势性**结论（非正式统计）：

| 要点 | 内容 |
|------|------|
| **对减小 \(K\) 的支持** | \(K=2\) 时 L0 的 `dt` 明显小于 \(K=3\)，说明**降低连续帧要求**有助于缩短等待；但 **\(K=2\) 与 \(K=3\) 两次 relocc 均未完成两层闭环**（`reloc_ok` 解析为 1、`task_ok` 失败），**不能**说明整体性能已改善。 |
| **更紧迫的问题** | 在边界扰动下，模块 C 可能已不仅是「更慢」，而是**影响完整跨层重定位与任务可完成性**；优化目标宜阶段性调整为**先恢复 `reloc_ok≥2` 与 `task_ok`**，再谈通过率质量与 FAR。 |
| **\(K\) 非唯一瓶颈** | 需联动排查 \(\tau\)、`reloc_min_check_delay`、超时与放宽机制、L0/L1 衔接及姿态抖动对连续帧判据的打断等。 |
| **下一轮最小实验方向** | 以 **\(K=2\)** 为试探基线；在失败机理查清前，优先**分析日志**而非盲目扩样本；可尝试 **\(K=2\)** + 缩短 delay / 略放宽 \(\tau\) / 超时放宽 等组合（见 `module_c_boundary_k_ablation_analysis.md` §5）。 |

全文与数据表见：**`docs/amcl/sp_amcl_experiments/module_c_boundary_k_ablation_analysis.md`**。

### 9.8 两层闭环失败的日志机理与参数优先级（最新）

对 K 消融 **`relocc_pert_0.32_0.32_0.2_run2/run3`** 的完整日志分析表明：主因是 **L1 上 `e_lin≈0.60 m` 与默认 `max_linear_error=0.5 m` 失配** → **`frame_ok` 长期为 0** → **无法形成第二次 `RELOC_PASS`**；在单帧始终不通过时，**K=2 与 K=3 不改变 L1 卡死结局**。据此应将 **`max_linear_error`、L1 分层门限、L1 超时放宽** 置于 **K 扫描** 之前。

完整论证、retries 来源、与 Baseline 对比及下一轮最小实验组合见：**`docs/amcl/sp_amcl_experiments/module_c_two_layer_closure_log_analysis.md`**。

